# Discord bot token from https://discord.com/developers/applications
DISCORD_TOKEN=your_bot_token_here

# Your bot's Application (Client) ID, from the same page
CLIENT_ID=your_application_id_here

# Optional: a Guild (server) ID for instant command registration during development.
# Leave blank to register commands globally (takes up to 1 hour to propagate).
GUILD_ID=
